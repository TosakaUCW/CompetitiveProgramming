#include <stdio.h>

#define REP(i,x,y) for(int i=x;i<=y;i++)
#define N 20

int a[N+5],n,k,ans[N+5],len_ans;

void dfs(int i){
	if(i==k+1)return;
//	REP(i,1,)
}

int main(){
	scanf("%d%d",&n,&k);
	REP(i,1,n)scanf("%d",&a[i]);
	dfs(1);
	return 0;
}
